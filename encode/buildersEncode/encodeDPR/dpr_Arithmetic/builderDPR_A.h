#ifndef BUILDER_DPR_ARITHMETIC_H
#define BUILDER_DPR_ARITHMETIC_H    
#include "stdint.h"

uint32_t builderADD(uint32_t value, char **saveptr);
uint32_t builderSUB(uint32_t value, char **saveptr);

#endif
